server:
  port: 8001

logging:
  level:
    com.bigos.app: DEBUG

spring:
  jpa:
    open-in-view: false
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5401/postgres?currentSchema=order&binaryTransfer=true&reWriteBatchedInserts=true
    username: postgres
    password: postgres